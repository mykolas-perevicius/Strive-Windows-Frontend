# Windows Thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Folder config file
Desktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msm
*.msp

# Windows shortcuts
*.lnk

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# IDE - IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws
out/
build/ # General build output, might conflict if Android uses 'build' differently

# Node Modules (at root, and potentially nested)
**/node_modules

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.*
!.env.example

# Build outputs (can be specific per project if needed)
dist
dist-ssr

# Optional: Android specific if not handled by android/.gitignore
# android/build/
# android/app/build/
# android/.gradle/
# android/local.properties
# android/app/google-services.json # If using Firebase